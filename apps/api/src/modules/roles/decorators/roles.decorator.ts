/**
 * Re-exportação do decorator Roles do módulo auth
 *
 * Este arquivo serve como wrapper para manter a consistência
 * e facilitar futuros ajustes sem quebrar importações existentes
 */
export { Roles } from '@nexus/auth';
