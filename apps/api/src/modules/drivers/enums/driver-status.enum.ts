/**
 * Driver Status Enum
 * Status possíveis para um motorista
 */
export enum DriverStatus {
  AVAILABLE = 'available',
  ON_ROUTE = 'on_route',
  UNAVAILABLE = 'unavailable',
  BLOCKED = 'blocked',
  VACATION = 'vacation',
}
