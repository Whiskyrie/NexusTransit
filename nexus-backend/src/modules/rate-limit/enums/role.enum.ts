/**
 * Role Enum (Temporário - será substituído pelo enum global)
 *
 * Roles hierárquicos para controle de acesso do rate-limit
 */
export enum Role {
  ADMIN = 'admin',
  GESTOR = 'gestor',
  DESPACHANTE = 'despachante',
  MOTORISTA = 'motorista',
  CLIENTE = 'cliente',
}
