/**
 * Role Type Enum
 * Tipos de papéis no sistema de autorização
 */
export enum RoleType {
  SUPER_ADMIN = 'super_admin',
  ADMIN = 'admin',
  MANAGER = 'manager',
  OPERATOR = 'operator',
  DRIVER = 'driver',
  CUSTOMER = 'customer',
}
