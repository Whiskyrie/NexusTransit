/**
 * User Type Enum
 * Tipos de usuário no sistema Nexus Transit
 * Mantém fidelidade total com o monólito
 */
export enum UserType {
  ADMIN = 'admin',
  DRIVER = 'driver',
  CUSTOMER = 'customer',
  OPERATOR = 'operator',
  MANAGER = 'manager',
}
